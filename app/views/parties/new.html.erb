<!-- Validation failure messages -->
<% if @party.errors.any? %>
  <% @party.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Party</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_party" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>
        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @party.name %>">
      </div>

      <!-- Label and input for date -->

      <div class="form-group">
        <label for="date" class="control-label">Date</label>
                <div class='input-group date' id='datetimepicker1'>
            <input name="date" type="text" class="form-control" id="date" placeholder="Date" value="<%= @party.date %>">
            <span class="input-group-addon">
              <i class="fa fa-calendar"></i>
            </span>
        <script type="text/javascript">
            $(function () { format:
                $('#datetimepicker1').datetimepicker({format: 'MM/DD/YYYY'});
            });
        </script>
          </div>
        </div>

      <!-- Label and input for duration -->
      <div class="form-group">
        <label for="duration" class="control-label">
          Duration
        </label>
        <input type="text" id="duration" name="duration" placeholder="Duration in hours" class="form-control" value="<%= @party.duration %>">
      </div>

      <!-- Label and input for male_count -->
      <div class="form-group">
      <table>
      <tr>
        <td><label for="male_count" class="control-label">
          Male Guests
        </label></td>
        <td><label for="female_count" class="control-label">
          Female Guests
        </label></td>
      </tr>
      <td>
        <input type="text" id="male_count" name="male_count" placeholder="Male guests" class="form-control" value="<%= @party.male_count %>"></td>
      <td>
        <input type="text" id="female_count" name="female_count" placeholder="Female guests" class="form-control" value="<%= @party.female_count %>"></td>
      </table>
    </div>

      <!-- Label and input for food_level -->
      <div class="form-group">
        <label for="food_level" class="control-label">
          Food level
        </label>

      <div id="slider">
        <input class = "bar" type="range" id="food_level" name="food_level" value="<%= @party.food_level %>" min = "0" max = "5" step = "1">
        <span class="highlight"></span>
        </div>
        </div>

      <!-- Label and input for drink_level -->
      <div class="form-group">
        <label for="drink_level" class="control-label">
          Drink level
        </label>

      <div id="slider">
        <input class = "bar" type="range" id="drink_level" name="drink_level" value="<%= @party.drink_level %>" min = "0" max = "5" step = "1">
        <span class="highlight"></span>
        </div>
        </div>

      <!-- Label and input for budget -->
      <div class="form-group">
        <label for="budget" class="control-label">
          Budget
        </label>
        <div class="input-group">
          <span class="input-group-addon">$</span>
            <input type="text" id="budget" name="budget" placeholder="Budget for all items purchase by host" class="form-control" value="<%= @party.budget %>">
          <span class="input-group-addon">.00</span>
        </div>
      </div>

      <!-- Label and input for appetizer_course -->



      <div class="form-group">
      <label for="courses" class="control-label">Courses</label>
      </div>

    <input type="hidden" id="appetizer_course" name="appetizer_course" value ="0">
    <input type="hidden" id="entree_course" name="entree_course" value ="0">
    <input type="hidden" id="dessert_course" name="dessert_course" value ="0">
    <input type="hidden" id="alcohol_course" name="alcohol_course" value ="0">


  <div class="btn-group" data-toggle="buttons">
    <label class="btn btn-primary active">
      <input type="checkbox" checked autocomplete="off" id="appetizer_course" name="appetizer_course" class="form-inline"  value ="1"> Appetizer
    </label>
    <label class="btn btn-primary active">
      <input type="checkbox" checked autocomplete="off" id="entree_course" name="entree_course" class="form-inline"  value ="1"> Entree
    </label>
    <label class="btn btn-primary active">
      <input type="checkbox" checked autocomplete="off" id="dessert_course" name="dessert_course" class="form-inline"  value ="1"> Dessert
    </label>
    <label class="btn btn-primary active">
      <input type="checkbox" checked autocomplete="off" id="alcohol_course" name="alcohol_course" class="form-inline"  value ="1"> Alcohol
    </label>
  </div>
  </div>
      <button class="btn btn-success">
        Create Party
      </button>
      or
      <a href="/parties">Cancel</a>
    </form>
  </div>

